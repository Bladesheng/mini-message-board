<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title: "Home"}) %>
  <body>
    <h1>Hello, I am <%= user.firstName + " " + user.lastName %></h1>
    <% if (user.admin) { %>
    <p>Let met tell you a secret: <b>I am an admin</b></p>

    <% } %> <%- include("../partials/footer") %>
  </body>
</html>
